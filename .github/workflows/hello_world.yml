name: 'Greeting'
on:
  push:
    branches:
      - 'main'
      - 'features/**'
jobs:
  terraformInit:
    name: 'terraform init'
    runs-on: 'Windows'
    steps:
      - name: 'greeting card'
        run: echo 'hello waghmare family'
    
